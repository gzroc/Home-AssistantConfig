  - platform: yeelight
    devices:
      192.168.0.31:
        name: bed_room_light
        transition: 1000
        use_music_mode: False #(defaults to False)
        save_on_change: True #(defaults to True)